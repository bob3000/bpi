---
- name: install packages
  apt:
    update_cache: yes
    upgrade: full
    name:
      - build-essential
      - cmake
      - curl
      - golang
      - liquidprompt
      - neovim
      - unzip
      - wget
